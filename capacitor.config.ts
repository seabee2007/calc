import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andrewterrell.concretecalc',
  appName: 'concrete-calculator-app',
  webDir: 'dist'
};

export default config;
