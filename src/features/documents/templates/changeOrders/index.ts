import type { DocumentTemplate } from '../../types';
import { GENERIC_CHANGE_ORDER_TEMPLATE } from '../../packs/changeOrder';

export const changeOrderTemplates: DocumentTemplate[] = [GENERIC_CHANGE_ORDER_TEMPLATE];
