/**
 * Phase 0.1 placeholder: token interpolation (Handlebars-style) is implemented
 * in a later phase. For now the raw template is returned unchanged.
 */
export function renderTemplate(
  template: string,
  _data: Record<string, unknown>,
): string {
  return template;
}
