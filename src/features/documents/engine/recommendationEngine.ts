import type { DocumentInput, DocumentRecommendation } from '../types';

/**
 * Phase 0.1 placeholder: clause/addendum recommendations are derived from
 * intake answers and risk scoring in a later phase. Returns none for now.
 */
export function recommendDocumentClauses(
  _input: DocumentInput,
): DocumentRecommendation[] {
  return [];
}
