import type { DocumentClause } from '../../types';

/** QC Report professional preview uses a dedicated renderer; no template clauses. */
export const qcReportPackClauses: DocumentClause[] = [];

export const qcReportPackClauseKeys: string[] = [];
