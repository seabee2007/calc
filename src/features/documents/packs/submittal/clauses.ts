import type { DocumentClause } from '../../types';

/** Submittal professional preview uses a dedicated renderer; no template clauses yet. */
export const submittalPackClauses: DocumentClause[] = [];

export const submittalPackClauseKeys: string[] = [];
