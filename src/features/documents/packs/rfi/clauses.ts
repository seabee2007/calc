import type { DocumentClause } from '../../types';

/** RFI professional preview uses a dedicated renderer; no template clauses yet. */
export const rfiPackClauses: DocumentClause[] = [];

export const rfiPackClauseKeys: string[] = [];
