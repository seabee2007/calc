import type { DocumentClause } from '../../types';

/** Daily Report professional preview uses a dedicated renderer; no template clauses. */
export const dailyReportPackClauses: DocumentClause[] = [];

export const dailyReportPackClauseKeys: string[] = [];
