/** Shared company logo sizing for professional document previews (contract, RFI, CO). */
export const DOCUMENT_HEADER_LOGO_CLASS =
  'mb-3 h-14 max-h-16 max-w-[120px] w-auto object-contain';

/** Approximate PDF logo dimensions (mm) — width × height. */
export const DOCUMENT_HEADER_LOGO_PDF_MM = { width: 48, height: 18 } as const;
